---
paths:
  - 'tests/Fixtures/AiProductLogic/**'
---

# Ai Product Logic

## Reuse shared AI product scenarios
Keep realistic Ukrainian evidence and semantic expectations in the shared JSON scenarios. Deterministic and opt-in live regressions must consume the same fixtures so prompt, model, schema, and pipeline changes are compared against one baseline.
