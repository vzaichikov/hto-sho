---
paths:
  - 'app/{Contracts,Data,Jobs,Services}/**/*Cart*.php'
---

# Contracts Data Jobs Services

## Silpo direct search and guarded diversification
Prepare every shopping need as a concrete retailer-facing purchasable identity and suitable form in both orchestrated and agentic modes; preserve its original menu role and purpose separately in note and plan context. Search that prepared identity first. After an empty or unsuitable result, consume the prepared retailer aliases in their declared order before PHP-derived single-word queries. Invoke the LLM product_name/purpose split only when that direct MCP call returns zero raw products; filtered-empty results do not qualify. Search the fallback product_name once with up to 30 results, adjudicate the lean candidate batch in one LLM decision, and never choose a role replacement while a viable exact-identity candidate remains. Keep application rules in developer/system instructions and runtime event/catalog data in the user message.
